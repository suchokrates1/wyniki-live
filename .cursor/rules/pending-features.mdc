---
description: Pending product work for wyniki-live and the umpire Android app. Apply when planning features, admin, overlay, or referee tablet sync.
alwaysApply: true
---

# Pending features

Canonical backlog: `docs/TODO.md`.

Do not treat backlog items as done until implemented and verified.

Shipped 2026-09-17 (Dell `:18087` + Android emulator):

1. **Director remote tablet control** — admin/reżyserka live-pushes court, names, score, and match rules onto the umpire app.
2. **Server-side match finish** — persist `MatchConfig` on create; auto-finish when the score satisfies the format; drop stale outbox 403 finishes.
