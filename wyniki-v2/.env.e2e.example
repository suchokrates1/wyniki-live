# E2E environment — copy to .env.e2e (gitignored) if needed.
# docker-compose.e2e.yml reads E2E_ADMIN_PASSWORD (not prod ADMIN_PASSWORD).

E2E_ADMIN_PASSWORD=e2e-admin
SECRET_KEY=e2e-secret
FLASK_ENV=production
