/**
 * Tailwind for the office and admin pages, built with Vite instead of the Play CDN.
 * Same as the CDN defaults (no plugins, default theme): DaisyUI still comes from its stylesheet.
 */
export default {
  content: ['./office.html', './admin.html', './src/**/*.js'],
};
