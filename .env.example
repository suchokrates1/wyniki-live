# Przykładowa konfiguracja zmiennych środowiskowych dla wyniki-live
# Skopiuj ten plik do .env i uzupełnij wartości zgodnie z poniższymi opisami.

# =============================================================================
# WYMAGANE USTAWIENIA
# =============================================================================

# Hasło do panelu administratora (wymagane dla dostępu do /admin)
# Jeśli nie ustawione, panel będzie wyświetlał informację o wyłączeniu
ADMIN_PASSWORD=

# Deprecated: Zachowane dla kompatybilności wstecznej
HISTORY_DELETE_PASSWORD=

# Uwierzytelnienie i komunikacja z Overlays
UNO_BASE=https://app.overlays.uno/apiv2/controlapps
UNO_AUTH_BEARER=

# Klucz sesji Flask (wygeneruj losowy string, np. openssl rand -hex 32)
# Wszystkie trzy zmienne są aliasami - wystarczy ustawić jedną
SECRET_KEY=
FLASK_SECRET_KEY=
SESSION_SECRET=

# =============================================================================
# PARAMETRY ODPYTYWANIA API
# =============================================================================

# Requests per minute (zapytań na minutę) na kort
RPM_PER_COURT=55

# Burst capacity dla rate limitera (ile zapytań naraz)
BURST=8

# =============================================================================
# KONFIGURACJA APLIKACJI
# =============================================================================

# Ścieżka do bazy danych SQLite
DB_PATH=wyniki_archive.sqlite3

# Port aplikacji
PORT=8080

# Rozmiar logów stanu w pamięci
STATE_LOG_SIZE=200

# Rozmiar historii meczów przechowywanych w pamięci
MATCH_HISTORY_SIZE=50

# =============================================================================
# USTAWIENIA UNO API - LIMITY GODZINOWE
# =============================================================================

# Limit godzinowy zapytań UNO na kort (0 = bez limitu, domyślnie 60)
UNO_HOURLY_LIMIT_PER_COURT=60

# Próg spowolnienia zapytań jako procent limitu (0.0-1.0, domyślnie 0.8 = 80%)
UNO_HOURLY_SLOWDOWN_THRESHOLD=0.8

# Współczynnik spowolnienia po przekroczeniu progu (domyślnie 2 = 2x wolniej)
UNO_HOURLY_SLOWDOWN_FACTOR=2

# Czas snu w sekundach podczas spowolnienia (domyślnie 1.0)
UNO_HOURLY_SLOWDOWN_SLEEP_SECONDS=1.0

# =============================================================================
# IDENTYFIKATORY OVERLAY DLA POSZCZEGÓLNYCH KORTÓW
# =============================================================================

# Każdy kort może mieć przypisany własny overlay ID
# Format: KORT{N}_ID gdzie N to numer kortu
KORT1_ID=
KORT2_ID=
KORT3_ID=
KORT4_ID=
# Dodaj kolejne zmienne KORT{N}_ID w razie potrzeby.

# =============================================================================
# OVERLAY BASE (opcjonalnie zamiast UNO_BASE)
# =============================================================================

# Bazowy URL dla overlay UNO (bez końcowego slasha)
# Jeśli ustawione, ma pierwszeństwo przed UNO_BASE
# OVERLAY_BASE=https://app.overlays.uno/apiv2/controlapps
